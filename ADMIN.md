login{
    email - admin@nocorps.in
    password - Aaaa@1234
}

page - verify farmer
list of farmers to be verified when clicking the former will show the entire details they filled in the end add accept and reject button

if accept then the farmer will be verified and can login send email about it
if reject(reject need remarks) then the farmer will not be verified and cannot login send email with remarks

page - category
add category, image(optional)
edit category
delete category

page - sub category
add sub category (under category) image(optional)
edit sub category
delete sub category

page - verify product
list of products to be verified when clicking the product will show the entire details they filled in the end add accept and reject button

if accept then the product will be verified and can login send email about it
if reject(reject need remarks) then the product will not be verified and cannot login send email with remarks